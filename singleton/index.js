import redButton from './redButton.js';
import blueButton from './blueButton.js';

document.addEventListener('DOMContentLoaded', () => {
  // initalize btns after the DOM is ready
  redButton();
  blueButton();
});
